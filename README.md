# SuShef Bot

Telegram-бот для автоматизации учёта накладных в ресторанном бизнесе.

## Описание

Бот принимает фото/PDF/текст накладной от шеф-повара, автоматически распознаёт данные через OCR и AI, парсит информацию о продуктах, ценах и поставщиках, сохраняет в базу данных и на Google Drive.

**Цель проекта:** Заменить ручной ввод данных по поставкам, обеспечить прослеживаемость закупок, упростить аналитику.

## Возможности

### Реализовано ✅
- **Обработка накладных** - фото, PDF, текстовый ввод
- **OCR** - распознавание текста через Google Gemini
- **AI парсинг** - извлечение структурированных данных (GPT-4)
- **Дедупликация** - проверка повторных загрузок по криптоключу
- **Хранение** - Google Drive (файлы) + Supabase (данные)
- **State management** - контекстные диалоги через состояния пользователя

### В разработке 🚧
- **Dashboard** - поиск накладных через естественный язык
- **Аналитика** - отчёты по поставкам и расходам
- **Web App** - веб-интерфейс для просмотра данных

## Технологии

- **Backend:** Node.js v24+
- **Bot Framework:** node-telegram-bot-api (polling mode)
- **Database:** Supabase (PostgreSQL + REST API)
- **Automation:** n8n (self-hosted на Railway)
- **AI:** Google Gemini (OCR), OpenAI GPT-4 (парсинг)
- **Storage:** Google Drive API
- **Version Control:** Git

## Архитектура

```
Telegram User
    ↓
Telegram Bot API
    ↓
Bot (Node.js) ← state management → Supabase (user_states)
    ↓
n8n Webhook (Railway)
    ↓
┌─────────────────────────┐
│ OCR (Gemini/Extract)    │
│ GPT Parsing (структура) │
│ Crypto Key (дедупл.)    │
│ Google Drive (файлы)    │
│ Supabase (данные)       │
└─────────────────────────┘
```

## Установка

### Требования
- Node.js v20+
- npm v10+
- Git
- Telegram Bot Token (от @BotFather)
- Supabase аккаунт
- n8n инстанс (Railway или self-hosted)
- Google Drive API credentials

### Шаги

1. **Клонировать репозиторий:**
```bash
git clone https://github.com/Max/sushef-bot.git
cd sushef-bot
```

2. **Установить зависимости:**
```bash
npm install
```

3. **Настроить переменные окружения:**

Создай файл `.env` в корне проекта:
```env
TELEGRAM_BOT_TOKEN=your_bot_token_from_botfather
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_KEY=your_anon_public_key
N8N_WEBHOOK_URL=https://your-n8n.railway.app/webhook/your-webhook-id
```

4. **Создать таблицы в Supabase:**

Выполни SQL из файла `create_user_states_table.sql`:
```sql
CREATE TABLE user_states (
  user_id BIGINT PRIMARY KEY,
  current_state TEXT DEFAULT 'idle',
  context JSONB DEFAULT '{}',
  created_at TIMESTAMP DEFAULT NOW(),
  updated_at TIMESTAMP DEFAULT NOW()
);
```

5. **Настроить n8n workflow:**
- Импортируй workflow "kitchen" в n8n
- Настрой Webhook node (Response Mode: Immediately)
- Подключи Google Drive, Supabase credentials
- Активируй workflow

6. **Запустить бота:**
```bash
node bot.js
```

Должно появиться: `🤖 Бот SuShef запущен!`

## Использование

### Команды бота

- `/start` - Начать работу, показать доступные команды
- `/supply` - Режим добавления накладной (отправь фото/PDF/текст)
- `/dashboard` - Поиск и аналитика (в разработке)

### Пример работы

1. Отправь боту `/supply`
2. Получи инструкцию
3. Отправь фото накладной
4. Бот обработает → сохранит → ответит "✅ Товар добавлен!"

## Структура проекта

```
sushef-bot/
├── bot.js                          # Основной код бота
├── state.js                        # State management (Supabase)
├── n8n.js                          # Интеграция с n8n
├── package.json                    # Зависимости
├── .env                            # Переменные окружения (не в Git)
├── .gitignore                      # Исключения для Git
├── create_user_states_table.sql   # SQL для Supabase
└── README.md                       # Эта документация
```

## Формат данных

### Входящие данные (от пользователя)
- **Фото:** JPEG/PNG накладной
- **PDF:** PDF документ
- **Текст:** Свободный формат (менее точно)

### Обработанные данные (в Supabase)
```json
{
  "поставщик": "ТОО Океан",
  "наименование_продукта": "Креветки королевские",
  "количество": 5.0,
  "единица_измерения": "кг",
  "цена_за_единицу": 12000.00,
  "дата_поставки": "2026-02-12"
}
```

## Как это работает

### Поток обработки накладной:

1. **Пользователь** отправляет фото/PDF накладной
2. **bot.js** скачивает файл через Telegram API
3. **bot.js** конвертирует в base64
4. **bot.js** отправляет POST запрос в n8n webhook
5. **n8n** конвертирует base64 → binary
6. **n8n** извлекает текст (Gemini OCR для фото, Extract from File для PDF)
7. **n8n** парсит текст через GPT-4 (структурированный JSON)
8. **n8n** генерирует crypto_key (SHA256) для дедупликации
9. **n8n** проверяет дубликат в Supabase
10. **Если новый:** загружает на Google Drive + записывает в Supabase
11. **Если дубликат:** отправляет уведомление "Файл уже загружен"

### State Management:

Бот использует состояния для контекстных диалогов:
- `idle` - ожидание команды
- `supply` - режим приёма накладных
- `dashboard` - режим поиска (в разработке)

Состояния хранятся в Supabase (таблица `user_states`).

## Разработка

### Запуск в dev режиме:
```bash
node bot.js
```

### Логи:
Бот выводит подробные логи в консоль:
```
🤖 Бот SuShef запущен!
👤 Пользователь 123456 запустил бота
📦 Пользователь 123456 → режим SUPPLY
📷 Фото от 123456, состояние: supply
📥 Скачивание файла: ABC...
✅ Файл скачан: 245.67 KB
📡 Отправка в n8n...
✅ n8n ответил: 200
```

### Git workflow:
```bash
# Сохранить изменения
git add .
git commit -m "feat: описание изменений"
git push

# Посмотреть историю
git log --oneline
```

## Deploy

### Локальный запуск (разработка):
```bash
node bot.js
```
Бот работает пока терминал открыт.

### Постоянный запуск (продакшн):

**Вариант 1: PM2**
```bash
npm install -g pm2
pm2 start bot.js --name sushef-bot
pm2 save
pm2 startup
```

**Вариант 2: Railway**
- Подключи GitHub репозиторий
- Настрой переменные окружения
- Deploy автоматически при push

**Вариант 3: VPS/Laptop**
- Используй systemd или screen для фонового запуска

## Troubleshooting

### Бот не отвечает
- Проверь токен в `.env`
- Убедись что бот запущен (`node bot.js`)
- Проверь логи в терминале

### "Ошибка при вызове n8n"
- Проверь `N8N_WEBHOOK_URL` в `.env`
- Убедись что n8n workflow активен
- Проверь доступность Railway (если используется)

### "Cannot read properties of undefined"
- Проверь структуру данных в n8n workflow
- Убедись что Switch правила корректны: `{{ $json.body.type }}`

### Файлы не загружаются на Google Drive
- Проверь credentials в n8n
- Убедись что folder_id правильный
- Проверь права доступа к папке

## Roadmap

### v1.0 (текущая версия) ✅
- Обработка накладных (фото/PDF/текст)
- State management
- Дедупликация
- Google Drive + Supabase

### v1.1 (в разработке) 🚧
- Dashboard (поиск через естественный язык)
- Очистка таблиц Google Sheets
- Улучшенная обработка ошибок

### v2.0 (планируется) 📋
- Web App интерфейс
- Аналитика и отчёты
- Multi-tenant (несколько ресторанов)
- Telegram Mini App

## Лицензия

Проект для внутреннего использования.

## Контакты

GitHub: [Max/sushef-bot](https://github.com/Max/sushef-bot)

---

**Последнее обновление:** Февраль 2026